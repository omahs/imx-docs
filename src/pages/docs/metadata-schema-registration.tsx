import React from 'react';
import { Redirect } from '@docusaurus/router';

export default function MetadataSchemaRegistration() {
  return <Redirect to="/docs/launch-collection/register-metadata-schema" />;
}
