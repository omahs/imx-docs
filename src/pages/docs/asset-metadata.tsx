import React from 'react';
import { Redirect } from '@docusaurus/router';

export default function AssetMetadata() {
  return (
    <Redirect to="/docs/launch-collection/register-metadata-schema#asset-metadata-and-how-its-used" />
  );
}
