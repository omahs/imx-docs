---
id: "imx-wallets-sdk-web"
title: "imx-wallets-sdk-web"
slug: "/imx-wallets-sdk-web"
description: "imx-wallets-sdk-web"
---

<!-- NOTE: Temporary redirection just to be used until merge the PR to main (will be removed before goes to main) -->

# Temporary redirection

import {Redirect} from '@docusaurus/router';

const Home = () => {
  return <Redirect to="/sdk-docs/wallets-sdk-web" />;
};
